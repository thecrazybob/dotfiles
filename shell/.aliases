# PHP/Laravel
alias a="php artisan"
alias tinker="php artisan tinker"
alias mfs='php artisan migrate:fresh --seed'
alias pp="php artisan test --parallel"
alias phpunit="vendor/bin/phpunit"
alias pint="vendor/bin/pint --dirty --parallel"
alias stan="vendor/bin/phpstan analyse"
alias sail='./vendor/bin/sail'
alias aoc="php artisan optimize:clear"
alias livewire="php artisan livewire"

# PHP version switching
alias switch-php83="brew unlink php@8.4 && brew link --overwrite --force php@8.3"
alias switch-php84="brew unlink php@8.3 && brew link --overwrite --force php@8.4"

# Composer
alias c="composer"
alias cu="composer update"
alias cr="composer require"
alias ci="composer install"
alias cda="composer dump-autoload -o"

# JavaScript
alias jest="./node_modules/.bin/jest"
alias ni="npm install"
alias nrd="npm run dev"
alias nrp="npm run prod"
alias nrw="npm run watch"

# Git
alias gc="git checkout"
alias gpo="git push origin"
alias gm="git merge"
alias glog="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias uncommit="git reset --soft HEAD~1"
alias nah='git reset --hard;git clean -df'

# System
alias l="ls -laF"
alias o="open ."
alias show="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"
alias hide="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"
alias ip="curl ifconfig.me/ip ; echo"
alias localip="ifconfig -a | grep -o 'inet6\? \(addr:\)\?\s\?\(\(\([0-9]\+\.\)\{3\}[0-9]\+\)\|[a-fA-F0-9:]\+\)' | awk '{ sub(/inet6? (addr:)? ?/, \"\"); print }'"
alias flushdns="sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder"
alias afk="osascript -e 'tell application \"System Events\" to keystroke \"q\" using {command down,control down}'"
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; sudo rm -rfv ~/.Trash; sudo rm -rfv /private/var/log/asl/*.asl"
alias sudo='sudo '

# Dev tools
alias phpstorm='open -a /Applications/PhpStorm.app "`pwd`"'
alias hostfile="sudo nano /etc/hosts"
alias sshconfig="nano ~/.ssh/config"
alias copykey='command cat ~/.ssh/id_rsa.public | pbcopy'
alias flush-redis="redis-cli FLUSHALL"

# Logs
alias php-log="open ~/.config/valet/Log/php-fpm.log"
alias nginx-log="open ~/.config/valet/Log/nginx-error.log"

# Claude
alias claude-usage='npx ccusage blocks --live'
alias claude-vacatia="CLAUDE_CONFIG_DIR=~/.claude-vacatia claude --dangerously-skip-permissions"
alias ccd="claude --dangerously-skip-permissions"
